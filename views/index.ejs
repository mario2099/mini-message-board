<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style-messages.css' />
  </head>
  <body>
    <h1 class="pageTitle title"><%= title %></h1>
    <p class="secondaryTitle title">Welcome to <%= title %></p>
    <a class="submit formEntry" href="/new">Submit a new message</a>
    <ul>
        <% if (messages.length > 0) {%>
          <% messages.forEach(messages =>{%>
            <li><%=messages.user%></li>
            <li><%=messages.text%></li>
            <li><%=messages.added%></li>
            <% })} %>
    </ul>
  </body>
</html>